apiVersion: v1
kind: Pod
metadata:
  name: secretpod
  namespace: namespace
  labels:
    name: roboshop
spec:
  containers:
  - name: nginx
    image: nginx:latest
    envForm:
      - SecretRef:
          name: secret