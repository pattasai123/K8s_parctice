apiVersion: v1
kind: Service
metadata:
  name: service
spec:
  type: NodePort
  selector: 
    names: roboshop
    env: dev
    language: yaml
    creator: balaji
  ports:
  - protocol: TCP
    port: 80 # service port
    targetPort: 80

  